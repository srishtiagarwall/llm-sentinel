// Re-exported here so consumers of @llm-sentinel/audit don't need a separate
// dependency on @llm-sentinel/tracing just to verify chain integrity.
export { verifyHashChain } from '@llm-sentinel/tracing';
